import { createStore } from 'vuex';
import notify from './notify';

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        notify,
    }
});
